<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tencent.wxcloudrun.dao.DailySummaryMapper">
    <insert id="insert" parameterType="com.tencent.wxcloudrun.model.DailySummary">
        INSERT INTO daily_summaries (date, content, created_at, updated_at)
        VALUES (#{date}, #{content}, NOW(), NOW())
        ON DUPLICATE KEY UPDATE
        content = #{content},
        updated_at = NOW()
    </insert>
    
    <select id="findByDate" resultType="com.tencent.wxcloudrun.model.DailySummary">
        SELECT id, date, content
        FROM daily_summaries
        WHERE date = #{date}
    </select>
</mapper>
